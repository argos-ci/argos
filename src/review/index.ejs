<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <!-- Use minimum-scale=1 to enable GPU rasterization -->
    <meta
      name="viewport"
      content="user-scalable=0, initial-scale=1, maximum-scale=1, minimum-scale=1, width=device-width, height=device-height"
    />

    <link rel="shortcut icon" href="/favicon.ico" />

    <!-- Android manifest -->
    <link rel="manifest" href="/manifest.json" />
    <meta name="theme-color" content="<%= theme.palette.primary[500] %>" />

    <!-- iOS Allow web app to be run in full-screen mode -->
    <meta name="apple-mobile-web-app-capable" content="yes" />

    <!-- iOS Icon -->
    <link rel="apple-touch-icon" sizes="152x152" href="/icon/152x152.png" />

    <!-- SEO -->
    <title>Argos CI - Automate visual regression testing</title>
    <meta
      name="description"
      content="Argos's visual regression system gives you high confidence in doing change"
    />

    <link
      href="https://fonts.googleapis.com/css?family=Roboto:400,400italic,500,700"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/icon?family=Material+Icons"
      rel="stylesheet"
    />
  </head>
  <body>
    <div id="root"></div>
    <script
      async
      src="https://www.googletagmanager.com/gtag/js?id=UA-101358560-6"
    ></script>
    <script>
      window.dataLayer = window.dataLayer || []
      function gtag() {
        dataLayer.push(arguments)
      }
      gtag('js', new Date())
      gtag('config', '<%= config.get("googleAnalytics") %>')

      window.clientData = <%- clientData %>;
    </script>

    <% htmlWebpackPlugin.files.js.forEach((file) => { %>
    <script src="<%=file %>" defer></script>
    <% }); %>
  </body>
</html>

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <!-- Use minimum-scale=1 to enable GPU rasterization -->
    <meta
      name="viewport"
      content="user-scalable=0, initial-scale=1, maximum-scale=1, minimum-scale=1, width=device-width, height=device-height"
    />

    <link rel="shortcut icon" href="/favicon.ico" />

    <!-- Android manifest -->
    <link rel="manifest" href="/manifest.json" />
    <meta name="theme-color" content="<%= theme.palette.primary[500] %>" />

    <!-- iOS Allow web app to be run in full-screen mode -->
    <meta name="apple-mobile-web-app-capable" content="yes" />

    <!-- iOS Icon -->
    <link rel="apple-touch-icon" sizes="152x152" href="/icon/152x152.png" />

    <!-- SEO -->
    <title>Argos CI - Automate visual regression testing</title>
    <meta name="description" content="Argos's visual regression system gives you high confidence in doing change" />

    <link href="https://fonts.googleapis.com/css?family=Roboto:400,400italic,500,700" rel="stylesheet">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  </head>
  <body>
    <div id="root"></div>

    <script>
      // Google Analytics
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.defer=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','//www.google-analytics.com/analytics.js','ga');
      ga('create', {trackingId: '<%= config.get("googleAnalytics") %>'});

      window.clientData = <%- clientData %>;
    </script>

    <% htmlWebpackPlugin.files.js.forEach((file) => { %>
      <script src="<%=file %>" defer></script>
    <% }); %>
  </body>
</html>

// import actionTypes from 'redux/actionTypes';

function reducer(state, action) {
  if (state === undefined) {
    state = {};
  }

  switch (action.type) {
    default:
      return state;
  }
}

export default reducer;

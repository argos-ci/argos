/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
export const up = async (knex) => {
  await knex.raw('ALTER TABLE projects ALTER COLUMN "accountId" SET NOT NULL');
};

/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
export const down = async (knex) => {
  await knex.raw('ALTER TABLE projects ALTER COLUMN "accountId" DROP NOT NULL');
};
